# Optional. GA4 is not requested or loaded until a visitor explicitly opts in.
PUBLIC_GA_MEASUREMENT_ID=
# Optional public verification tokens. These are metadata, not secrets.
PUBLIC_GOOGLE_SITE_VERIFICATION=
PUBLIC_BING_SITE_VERIFICATION=
